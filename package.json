{
  "private": true,
  "scripts": {
    "test": "npm run lint && npm run build && ts-node test/index.ts",
    "build": "typings bundle . --out bundle",
    "lint": "tslint index.d.ts",
    "prepublish": "typings install"
  },
  "devDependencies": {
    "envobj": "^1.0.1",
    "mime": "^1.3.4",
    "ts-node": "^3.0.3",
    "tslint": "^5.2.0",
    "tslint-config-typings": "^0.3.1",
    "typescript": "^2.3.2",
    "typings": "^2.1.1"
  }
}
